export default class APIClient {

    /**
     * Receives a number of holes, creates a new game with that number 
     * of holes, redirects user to it
     * @param {number} numberOfHoles - number of holes in the game 
     */
    static createNewGame(numberOfHoles) {
        
        console.info("Calling API to create new game");
    }
}